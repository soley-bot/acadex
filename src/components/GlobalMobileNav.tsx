'use client'

import { usePathname } from 'next/navigation'

export function GlobalMobileNav() {
  // Mobile navigation removed - replaced with contextual page-level navigation
  // Each page now has its own contextual back buttons and navigation
  return null
}

